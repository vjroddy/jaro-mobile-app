# Backend (Express + TypeScript)

This folder contains the backend API scaffold for the JARO fullstack implementation. It includes a minimal Express server, Prisma schema, and payment webhook flow that activates users after a successful mobile-money payment.

See README in the root for how to run locally using docker-compose.
