# Admin scaffold

This folder will hold the admin panel (React + Vite + TypeScript). For now it's a placeholder scaffold with notes.

Run locally (when implemented):
- cd admin
- npm install
- npm run dev
